责任链模式
在处理对象执行某些操作前进行一系列的校验
使多个对象都有机会处理请求，从而避免了请求的发送者和接受者之间的耦合关系。将这些对象连城一条链，并沿着这条链传递该请求，直到有对象处理它为止


优点
1，将处理和请求分开，请求者可以不知道是谁处理的，处理者可以不知道请求的全貌


缺点
1，性能问题，责任链长的时候性能是非常大的问题
2，调试不方便

适用场景


注意
1，链中的节点数需要控制

