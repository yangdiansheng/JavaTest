策略模式
定义一组算法，将每个算法都封装起来，并且是他们之间可互换


优点
1，算法可自由切换
2，避免使用多重条件判断
3，扩展性良好

缺点
1，策略类数量增多
2，所有的策略类都需要对外暴露

使用场景
1，多个类只有在算法或行为上稍微有不同的场景
2，算法需要自由切换的场景
3，需要屏蔽算法规则的场景

注意
1，系统中策略家族具体策略数超过4个，则需考虑使用混合模式

策略枚举
1，方便优秀的模式，但是受枚举类型的限制
2，扩展性受到一定约束，策略枚举一般担当不经常发生变化的角色