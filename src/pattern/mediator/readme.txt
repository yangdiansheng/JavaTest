中介模式（调停模式）
用一个中介对象封装一系列的对象交互，中介者使个对象不需要显示地相互调用，从而使其耦合松散，而且可以独立地改变他们之间的交互


优点
1，减少了类之间的依赖，从一对多到一对一的依赖，同事类只依赖中介者，减少了依赖，降低了耦合

缺点
1，中介者会膨胀的很大，而且逻辑复杂

适用场景
1，类图中出现了蜘蛛网结构，这种情况考虑中介模式将蜘蛛网梳理成星型结构
2，mvp、中介服务

注意
