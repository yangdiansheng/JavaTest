建造者模式
生成器模式，将复杂对象的构建与它的表示分离，使得同样的构建过长可以创建不同的表示


优点
1，封装性
2，容易扩展
3，便于控制细节风险

缺点

使用场景
1，相同的方法，不同的执行顺序，产生不同的事件
2，多个不见或零件
3，产品类非常复杂，产品类中调用顺序不同产生了不同的效能
4，在创建过程中会使用到系统中的一些其他对象，这些对象创建过程中不易得到，可以采用建造者模式封装该对象的创建过程