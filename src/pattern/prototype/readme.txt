原型模式
通过原型创建多个副本对象
用原型实例指定创建对象的种类，并通过靠谱这些原型创建新的对象


优点
1，性能优良
2，逃避构造函数的约束


缺点
1，

适用场景
1，类初始化需要消耗非常多的资源，这个资源包括数据、硬件资源
2，性能和安全要求场景
3，一个对象多个修改这场景

注意
1，构造函数不会执行
2，浅拷贝（内部数组和引用对象才不拷贝）和深拷贝（对私有对象进行独立拷贝）
3，clone和final
